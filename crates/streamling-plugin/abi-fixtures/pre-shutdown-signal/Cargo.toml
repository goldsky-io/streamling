[package]
name = "abi-fixture-pre-shutdown-signal"
version = "0.0.0"
edition = "2024"
publish = false
description = "Plugin library with an older module layout, loaded by the host's compatibility tests."

[lib]
crate-type = ["cdylib"]
test = false
doctest = false

[dependencies]
streamling-plugin = { workspace = true }
abi_stable.workspace = true
